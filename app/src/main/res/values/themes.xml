<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Plain" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <!--
          Transparent window background: the Compose root paints the palette
          colour itself, which lets the "show wallpaper" setting work by simply
          dropping the background alpha at runtime.
        -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
